import { Metadata } from './metadata';
import { Response } from './reader/response';
export default class Reader<T extends Response> {
    metadata: Metadata;
    private decoder;
    private db;
    private walker;
    constructor(db: Buffer, opts?: object);
    load(db: Buffer, opts: object): void;
    get(ipAddress: string): T | null;
    findAddressInTree(ipAddress: string): number | null;
    resolveDataPointer(pointer: number): any;
}
